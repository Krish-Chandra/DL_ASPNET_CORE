@model Role

@{
    ViewBag.Title = "Edit Role";
    string[] claims = { "Authors", "Books", "Categories", "Publishers", "Requests", "Issues", "Roles" };
}

<div style="width:700px;">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <b>Edit Role</b>
        </div>
        <div class="panel-body">
            <form method="post" asp-antiforgery="true">
                <input type="hidden" asp-for="Id" />
                <div class="form-group">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="control-label">Permissions</label>
                    <br />
                    @foreach (var claim in claims)
                    {
                        <input type="checkbox" value="@claim" @(Array.IndexOf(ViewBag.roleClaims, claim) >= 0 ? "checked" : "") name="claims" />@claim
                        <br />
                    }

                </div>
                <div class="form-group">
                    <input type="submit" value="Update" class="btn btn-success" />
                    <a asp-action="Index" class="btn btn-warning">Cancel</a>
                </div>

            </form>

            @*@using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.Id)
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label" })
                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                </div>
                <div class="form-group">
                    <label class="control-label">Permissions</label>
                    <br />

                    @foreach (var claim in claims)
                    {
                        <input type="checkbox" value="@claim" @(Array.IndexOf(ViewBag.roleClaims, claim) >= 0 ? "checked" : "") name="claims" />@claim
                        <br/>
                    }

                </div>
                <div class="form-group">
                    <input type="submit" value="Update" class="btn btn-success" />
                    @Html.ActionLink("Cancel", "Index", new { }, new { @class = "btn btn-warning" })
                </div>
            }*@
        </div>
    </div>
</div>